export default function Header () {
  return (
    <header className="sticky top-0 w-full p-3 border-b border-slate-200 h-[50px] backdrop-blur">
      <h2 className="font-semibold">Inicio</h2>
    </header>
  )
}
